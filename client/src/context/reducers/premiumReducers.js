const premiumReducer = (state=null,action) => {
     switch(action.type){
        case "SET_PREMIUM_USER":
            return action.val
            case "GET_PREMIUM_USER":
                return state
                default : 
                return state
     }
}

export default premiumReducer